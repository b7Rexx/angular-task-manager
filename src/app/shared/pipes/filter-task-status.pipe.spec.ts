import { FilterTaskStatusPipe } from './filter-task-status.pipe';

describe('FilterTaskStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTaskStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
